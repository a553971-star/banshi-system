# 給未來 AI 助手的開場白模板

> 用法：開新對話時，從下方「複製這一段開始」到「複製到這裡結束」整段複製貼上。
> 這份模板的目的：讓新的 AI 助手在 30 秒內進入狀況，不用你每次重新解釋。

---

## 開場白模板（複製這一段開始）

```
我在開發「磐石決策系統」——一個台股量化決策平台。

請先讀這個專案的座標系：
https://github.com/a553971-star/banshi-system/blob/main/PROJECT_CONTEXT.md

讀完之後，再開始幫我做事。

【我的環境】
- macOS（MacBook Air）
- 工作目錄：/Users/hsc/Documents/banshi_system
- Python 3.9.6（重要：不能用 str | None，要用 Optional[str]）
- GitHub 用 SSH/HTTPS + Mac Keychain 認證

【主分支策略】
- 所有修改直接到 main，不要建新分支
- commit 訊息可用中文
- 大改動前先告訴我計畫、等我點頭

【動 banshi.db 前的鐵則】
- 先 cp banshi.db banshi.db.backup-$(date +%Y-%m-%d)
- 不要 commit banshi.db（讓本機與雲端各自演化）
- 不要硬合併 banshi.db 的衝突

【跟 TWSE 互動的限制】
- 本機抓 TWSE 常 timeout，這是已知環境限制不是 bug
- 緊急補洞失敗時推上 GitHub 讓 Actions 接力（美國 IP 通常順）

【溝通風格】
- 一步一步，每步告訴我「現在在哪個房間（pwd）」
- 用日常比喻解釋抽象概念
- 不直接給錯誤的修法——先讓我理解錯誤再修
- 我說「你太像工程師了」就請切回比喻模式
- 我不確定時會主動問你，請鼓勵這個習慣

【今天我想做的事】
（在這裡寫今天的任務，越具體越好）
```

## 複製到這裡結束

---

## 為什麼開場白要長這樣？

每個段落的設計理由：

### 「請先讀座標系」

讓 AI 主動拉取 GitHub 上的 `PROJECT_CONTEXT.md`，省下你重新解釋架構的時間。AI 看完那份文件，就知道 CBA、Universe、雙環境同步、踩過的坑。

### 「我的環境」

避免 AI 寫出本機跑不起來的程式碼。Python 3.9.6 是死線——任何 `str | None`、`match-case` 之類的新語法在你 Mac 上都會炸。

### 「主分支策略」

防止重演 2026-05-26 那次「修復推到舊分支、沒進 main」的事件。

### 「動 banshi.db 前的鐵則」

防止重演 `git restore` 弄丟資料、`cherry-pick` 撞到二進位衝突的事件。

### 「跟 TWSE 互動的限制」

防止 AI 把「TWSE timeout」當成 bug 拼命修——它不是 bug，是限流。

### 「溝通風格」

讓 AI 知道你的學習偏好。一步一步、確認位置、用比喻——不是工程師的硬塞。

### 「今天我想做的事」

具體任務寫越清楚，AI 越不會自由發揮。

---

## 常見任務的 SOP 範本

### 範本 A：跑緊急補洞

```
今天 banshi.db 的 MAX(date) 沒前進到昨天，可能有資料缺漏。
請帶我：
1. 查 banshi.db 缺哪幾天
2. 跑 backfill_db.py（如果本機 TWSE timeout 就推上去讓 Actions 跑）
3. 驗證補完之後 MAX(date) 對齊昨天
```

### 範本 B：修一個明確的 bug

```
我在 [檔案名] 第 [行號] 看到 bug，症狀是 [描述]。
請：
1. 先讀 PROJECT_CONTEXT.md 了解專案脈絡
2. 用 grep 定位相關程式碼，不要無腦讀整個檔案
3. 改之前告訴我計畫
4. 改完先本機跑驗證
5. 沒問題再 commit + push 到 main
```

### 範本 C：加新功能

```
我想加一個新功能：[描述]。
請：
1. 先讀 PROJECT_CONTEXT.md
2. 評估這個功能跟現有架構怎麼整合
3. 列出要改的檔案清單給我確認
4. 我點頭後再動手
5. 改完先在本機 Streamlit 跑起來驗證
6. 沒問題再 commit + push 到 main
```

### 範本 D：純諮詢，不動程式碼

```
我想跟你討論 [主題]，現階段不要動任何檔案。
先聽我描述完，再給我意見。
```

---

## 給未來的你自己的提醒

### 何時要更新 PROJECT_CONTEXT.md？

- 加入新檔案（更新「主要檔案地圖」）
- 加入新表（更新「`banshi.db` 主要表」）
- 踩到新坑（更新「已知問題與環境限制」）
- 換 Python 版本（更新「環境」段）
- 換看盤介面（更新第 2 節「介面」）
- 重大決策（更新「重大歷史事件」）

更新後 commit 訊息建議：`docs: update context (新增/修改 XX 段)`

### 何時不需要更新？

- 改 bug 細節（除非牽涉架構）
- 微調 UI 配色
- 改文案

### 對話換到一半，AI 表現變差怎麼辦？

- 不要硬撐——開新對話，貼這份模板，把進度說一次
- 上下文太長後 AI 會變笨，這不是模型問題、是上下文密度問題
- 換對話的成本（重新解釋）比硬撐的成本（被誤導）低

---

*本模板最後更新：2026-05-26*
